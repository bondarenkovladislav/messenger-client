// const env = process.env.NODE_ENV;
const params = { API_HOST: "http://messenger.magazento.com/api/messenger" }; // eslint-disable-line import/no-dynamic-require
//
// console.log("HOST --->", params.API_HOST);
// console.log("ENV --->", env);

export default params;
